<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Create New Turtle</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <h1>Create New Turtle:</h1>
        <% if (typeof(errors) != 'undefined') { %>
            <h4>ERRORS:</h4>
            <% for (var error in errors) { %>
                <p><%= errors[error].message %></p>
            <% } %>
        <% } %>
        <form action="/turtles" method="post">
            <p>
                <label for="name">Turtle name: </label>
                <input type="text" name="name" id="name" value=<%= prefill.name %>>
            </p>
            <p>
                <label for="color">Turtle color: </label>
                <input type="text" name="color" id="color" value=<%= prefill.color %>>
            </p>
            <p>
                <input type="submit" value="Create turtle!">
            </p>
        </form>
        <p><a href="/">Home</a></p>
    </body>
</html>